<?xml version="1.0" encoding="UTF-8"?>
<template xmlns="http://ws.apache.org/ns/synapse" name="getFollowers">

    <!-- Optional Parameters -->
    <parameter name="meta" description="Loads data from the endpoints found in the 'meta' part of the response"/>
    <parameter name="fields" description="Return only the specified fields"/>
    <parameter name="page" description="page number Default is 1"/>
    <parameter name="number" description="number of followers to return Default is 20"/>

    <sequence>

        <property name="uri.var.meta" expression="$func:meta" scope="default" type="STRING"/>
        <property name="uri.var.fields" expression="$func:fields" scope="default" type="STRING"/>
        <property name="uri.var.page" expression="$func:page" scope="default" type="STRING"/>
        <property name="uri.var.number" expression="$func:number" scope="default" type="STRING"/>

        <filter
                xpath="(not(get-property('uri.var.meta') = '' or (not(string(get-property('uri.var.meta'))))))">
            <then>
                <property name="uri.var.queryParameters"
                          expression="fn:concat(get-property('uri.var.queryParameters'),'meta=',get-property('uri.var.meta'),'&amp;')" />
            </then>
        </filter>

        <filter
                xpath="(not(get-property('uri.var.fields') = '' or (not(string(get-property('uri.var.fields'))))))">
            <then>
                <property name="uri.var.queryParameters"
                          expression="fn:concat(get-property('uri.var.queryParameters'),'fields=',get-property('uri.var.fields'),'&amp;')" />
            </then>
        </filter>
        <filter
                xpath="(not(get-property('uri.var.page') = '' or (not(string(get-property('uri.var.page'))))))">
            <then>
                <property name="uri.var.queryParameters"
                          expression="fn:concat(get-property('uri.var.queryParameters'),'page=',get-property('uri.var.page'),'&amp;')" />
            </then>
        </filter>

        <filter
                xpath="(not(get-property('uri.var.number') = '' or (not(string(get-property('uri.var.number'))))))">
            <then>
                <property name="uri.var.queryParameters"
                          expression="fn:concat(get-property('uri.var.queryParameters'),'number=',get-property('uri.var.number'),'&amp;')" />
            </then>
        </filter>

        <property name="uri.var.wp_resource_uri" expression="fn:concat('https://public-api.wordpress.com/rest/v1/sites/',get-property('uri.var.wp_site_domain'),'/follows/')" scope="default" type="STRING"/>

        <call>
            <endpoint>
                <http method="GET" uri-template="{uri.var.wp_resource_uri}?{uri.var.queryParameters}" />
            </endpoint>
        </call>

    </sequence>
</template>
